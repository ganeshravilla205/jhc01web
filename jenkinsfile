pipeline{
    agent any
    stages{
        stage("Maven Build"){
            steps{
                sh "mvn clean package"
                }
             }
         stage("deploy war file to nexus"){
            steps{
                sh "mvn clean package"
            }
       }
    }
}
